services:
  - type: web
    name: movie-expert-system
    env: node
    buildCommand: cd public/dashboard && npm install --legacy-peer-deps && npm run build && cd ../.. && npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
